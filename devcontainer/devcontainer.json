{
    "name": "Baixador de Midias",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg && curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb && sudo dpkg -i cloudflared.deb && rm cloudflared.deb"
}